{
  "name": "dsp-resource-sheet",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "preview": "npm run preview --workspace client -- --host 127.0.0.1",
    "start": "npm run preview",
    "lint": "npm run lint --workspace client"
  }
}
